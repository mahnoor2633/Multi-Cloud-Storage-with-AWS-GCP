# Multi-Cloud-Storage-with-AWS-GCP
A secure, automated backup pipeline that transfers objects from AWS S3 to Google Cloud Storage using Google Storage Transfer Service with federated identity (short-lived credentials) for safer cross-cloud access, availability and disaster recovery.
